function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  
  rect(125,170,150,40)
  textAlign(CENTER)
  textSize(25);
  text("Project FMS", 200, 200)
  
  rect(140,280,120,30)
  textAlign(CENTER)
  textSize(15);
  text("Game Selection", 200, 300)
 
}
