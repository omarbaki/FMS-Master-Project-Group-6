function setup() {
  createCanvas(700, 600);
  background('#0096FF');
  button = createButton();
  button.position(100, 500);
  button.mousePressed(settingsPage);
}

function settingsPage(){
  background('#FF9800'); 
}
